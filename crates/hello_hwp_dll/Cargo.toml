[package]
name = "hello_hwp_dll"
version = "0.1.0"
license = "MIT"
edition = "2024"
publish = false

[lib]
name = "hello_hwp_dll"
crate-type = ["cdylib"]

[dependencies]
hwp_addon = { path = "../hwp_addon" }
hwp_core = { path = "../hwp_core" }
